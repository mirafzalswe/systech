# 🚀 Гайд по запуску приложения

## Предварительные требования

- **Python 3.8+**
- **pip** (менеджер пакетов Python)

## Шаг 1️⃣: Установка зависимостей

Откройте терминал и перейдите в папку проекта:

```bash
cd /Users/Macbook/Desktop/Test_project
```

Установите требуемые пакеты:

```bash
pip install -r requirements.txt
```

Или установите Django напрямую:

```bash
pip install Django==4.2
```

## Шаг 2️⃣: Выполнение миграций

Создайте таблицы в базе данных:

```bash
python manage.py makemigrations
python manage.py migrate
```

Если используется Python 3:

```bash
python3 manage.py makemigrations
python3 manage.py migrate
```

Вы должны увидеть:
```
Migrations for 'test_pr':
  test_pr/migrations/0001_initial.py
    - Create model Test
    - Create model Question
    - Create model Answer
    - Create model Participant
    - Create model TestResult
    - Create model UserAnswer
```

## Шаг 3️⃣: Создание администратора (суперпользователя)

```bash
python manage.py createsuperuser
```

или

```bash
python3 manage.py createsuperuser
```

Следуйте указаниям:
```
Username: admin
Email address: admin@example.com
Password: ••••••••
Password (again): ••••••••
```

⚠️ **Запомните учетные данные!** Они нужны для входа в админ-панель.

## Шаг 4️⃣: Запуск сервера

### Вариант 1️⃣: Простой способ (Linux/Mac)

```bash
chmod +x run.sh
./run.sh
```

### Вариант 2️⃣: Ручной запуск

```bash
python manage.py runserver
```

или

```bash
python3 manage.py runserver
```

Вы должны увидеть:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

## Шаг 5️⃣: Открытие приложения

### 👤 Пользовательская часть:
[http://localhost:8000/](http://localhost:8000/)

### 🔐 Админ-панель:
[http://localhost:8000/admin/](http://localhost:8000/admin/)

Введите учетные данные администратора, которые создали на шаге 3.

## 📝 Создание первого теста

1. Откройте админ-панель (http://localhost:8000/admin/)
2. В левом меню нажмите **"Тесты"**
3. Нажмите кнопку **"ДОБАВИТЬ ТЕСТ"**
4. Заполните поля:
   - **Название теста**: например, "Математика - Уровень 1"
   - **Описание**: "Базовые вопросы по математике"
   - **Статус**: выберите "Активен"
   - **Показывать правильные ответы**: "После каждого вопроса"
   - **Показывать результат в конце**: включите галочку
   - **Таймер (в минутах)**: 30 (опционально)
5. Нажмите **"СОХРАНИТЬ"**

## ❓ Добавление вопросов

После создания теста:

1. В той же форме теста найдите секцию **"Вопросы"**
2. Нажмите **"Добавить еще один Вопрос"**
3. Заполните:
   - **Текст вопроса**: "Сколько будет 2 + 2?"
   - **Порядок**: 1
4. Нажмите **"СОХРАНИТЬ"**

## ✅ Добавление ответов

После добавления вопроса:

1. В левом меню нажмите **"Вопросы"**
2. Найдите и откройте ваш вопрос
3. Найдите секцию **"Ответы"**
4. Добавьте ответы:
   - **Ответ 1**: "3" (не отмечайте как правильный)
   - **Ответ 2**: "4" (отметьте как правильный ✓)
   - **Ответ 3**: "5" (не отмечайте как правильный)
5. Нажмите **"СОХРАНИТЬ"**

## 🧪 Тестирование

1. Откройте [http://localhost:8000/](http://localhost:8000/)
2. Введите имя и фамилию
3. Выберите созданный тест
4. Ответьте на вопросы
5. Нажмите "Завершить тест"
6. Просмотрите результаты

## 🔍 Просмотр результатов

1. Откройте админ-панель
2. В левом меню выберите **"Результаты тестов"**
3. Вы увидите список всех пройденных тестов с:
   - Именем участника
   - Названием теста
   - Количеством правильных ответов
   - Процентом успеха
   - Датой прохождения

## ⚙️ Возможные ошибки и решения

### Ошибка: "ModuleNotFoundError: No module named 'django'"
**Решение**: Установите Django через pip:
```bash
pip install Django
```

### Ошибка: "No such table: test_pr_test"
**Решение**: Выполните миграции:
```bash
python manage.py migrate
```

### Ошибка: "Permission denied" при запуске run.sh
**Решение**: Дайте права на исполнение:
```bash
chmod +x run.sh
```

### Порт 8000 уже занят
**Решение**: Запустите на другом порту:
```bash
python manage.py runserver 8080
```

## 📊 Структура БД

При первом запуске создаются следующие таблицы:

| Таблица | Описание |
|---------|----------|
| test_pr_test | Хранит информацию о тестах |
| test_pr_question | Вопросы тестов |
| test_pr_answer | Варианты ответов |
| test_pr_participant | Участники тестирования |
| test_pr_testresult | Результаты прохождения тестов |
| test_pr_useranswer | Ответы пользователей на вопросы |

## 💡 Полезные команды

```bash
# Просмотр всех миграций
python manage.py showmigrations

# Откат миграции
python manage.py migrate test_pr 0001

# Сброс БД (внимание: удалит все данные!)
rm db.sqlite3
python manage.py migrate

# Создание другого суперпользователя
python manage.py createsuperuser

# Запуск shell для работы с БД
python manage.py shell

# Сбор статических файлов
python manage.py collectstatic
```

## 🎯 Основные URL маршруты

| URL | Описание |
|-----|----------|
| `/` | Регистрация пользователя |
| `/tests/` | Список доступных тестов |
| `/test/<id>/take/` | Прохождение теста |
| `/result/<id>/` | Просмотр результата |
| `/logout/` | Выход пользователя |
| `/admin/` | Админ-панель |

## 🆘 Нужна помощь?

Если что-то не работает:

1. **Проверьте Python**: `python --version`
2. **Проверьте Django**: `python -c "import django; print(django.VERSION)"`
3. **Проверьте консоль**: В терминале будут сообщения об ошибках
4. **Смотрите логи**: Django выводит детальные ошибки в консоль

---

**Всё готово! Наслаждайтесь использованием приложения! 🎉**
