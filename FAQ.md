# ❓ Часто задаваемые вопросы (FAQ)

## Установка и запуск

### ❓ Как установить приложение?

**Ответ**: Следуйте инструкциям в файле `SETUP.md`:

1. Установите Django: `pip install Django`
2. Выполните миграции: `python manage.py migrate`
3. Создайте администратора: `python manage.py createsuperuser`
4. Запустите сервер: `python manage.py runserver`

### ❓ Какая версия Python требуется?

**Ответ**: Python 3.8 или выше. Проверьте версию:
```bash
python --version
```

### ❓ Я получаю ошибку "command not found: python"

**Ответ**: Используйте `python3` вместо `python`:
```bash
python3 manage.py runserver
```

### ❓ Как мне узнать IP адрес компьютера?

**Ответ**: 
- **Mac/Linux**: `ifconfig | grep "inet "`
- **Windows**: `ipconfig`

Или запустите сервер с флагом `0.0.0.0`:
```bash
python manage.py runserver 0.0.0.0:8000
```

---

## Работа с тестами

### ❓ Как создать тест?

**Ответ**:
1. Откройте [http://localhost:8000/admin/](http://localhost:8000/admin/)
2. Нажмите "Тесты" → "Добавить тест"
3. Заполните название, описание, выберите статус "Активен"
4. В этой же форме добавьте вопросы
5. Для каждого вопроса добавьте ответы

### ❓ Какой правильный ответ в тесте?

**Ответ**: В админ-панели, при добавлении ответа, отметьте галочкой "Правильный ответ" для верного варианта.

### ❓ Могу ли я добавить несколько правильных ответов?

**Ответ**: В текущей версии - **НЕТ**. Это требует переработки логики. Но вы можете добавить в вопрос текст типа "Выберите ВСЕ правильные ответы" (дополнительная работа со скриптом).

### ❓ Как сделать тест без таймера?

**Ответ**: При создании теста оставьте поле "Таймер (в минутах)" пустым.

### ❓ Что делать, если я сделал ошибку при создании вопроса?

**Ответ**: 
1. Откройте админ-панель
2. Найдите тест → Нажмите на вопрос
3. Отредактируйте текст
4. Нажмите "Сохранить"

Или удалите вопрос полностью и создайте новый.

### ❓ Как упорядочить вопросы в правильном порядке?

**Ответ**: Каждый вопрос и ответ имеет поле "Порядок". Установите:
- Вопрос 1 → order = 1
- Вопрос 2 → order = 2
- и т.д.

---

## Прохождение тестов (Пользователь)

### ❓ Как начать прохождение теста?

**Ответ**:
1. Откройте [http://localhost:8000/](http://localhost:8000/)
2. Введите ваше имя и фамилию
3. Нажмите "Начать тестирование"
4. Выберите тест и нажмите "Начать"

### ❓ Я забыл свое имя при регистрации. Что делать?

**Ответ**: Нажмите "Выход" и зарегистрируйтесь заново с правильными данными.

### ❓ Могу ли я пропустить вопрос и ответить позже?

**Ответ**: **ДА!** Вы можете:
- Кликнуть на номер другого вопроса
- Использовать кнопки "Следующий" / "Предыдущий"
- Вернуться и отредактировать ответ

### ❓ Что будет, если время закончится?

**Ответ**: Тест автоматически завершится и вы увидите результаты.

### ❓ Могу ли я пройти тест дважды?

**Ответ**: **НЕТ**. Один участник может пройти каждый тест только один раз. Результат сохраняется в системе.

### ❓ Как я увижу результаты?

**Ответ**: После завершения теста вы сразу увидите:
- Количество правильных ответов
- Процент успеха
- Подробный разбор (если включено администратором)

### ❓ Почему я не вижу правильные ответы?

**Ответ**: Администратор может скрыть правильные ответы (выбрал "Не показывать"). Свяжитесь с администратором.

---

## Результаты и статистика

### ❓ Где я могу увидеть результаты всех участников?

**Ответ**: В админ-панели:
1. [http://localhost:8000/admin/](http://localhost:8000/admin/)
2. Левое меню → "Результаты тестов"

### ❓ Как экспортировать результаты?

**Ответ**: 
1. В админ-панели → "Результаты тестов"
2. Копируйте данные и вставьте в Excel/Google Sheets

Или используйте команду Django Shell:
```bash
python manage.py shell
```

```python
from test_pr.models import TestResult
import csv

results = TestResult.objects.all()
with open('results.csv', 'w') as f:
    writer = csv.writer(f)
    for r in results:
        writer.writerow([
            r.participant.first_name,
            r.participant.last_name,
            r.test.title,
            r.correct_answers,
            r.total_questions,
            r.percentage
        ])
```

### ❓ Можно ли удалить результат теста?

**Ответ**: 
- **Через админ-панель**: НЕТ (запретили для безопасности)
- **Через БД**: ДА, используя `python manage.py shell` и `.delete()`

### ❓ Как отфильтровать результаты по тесту?

**Ответ**: В админ-панели → "Результаты тестов" → справа "Фильтры" → выберите тест

---

## Ошибки и проблемы

### ❓ Ошибка: "No such table: test_pr_test"

**Ответ**: Вы не выполнили миграции. Запустите:
```bash
python manage.py makemigrations
python manage.py migrate
```

### ❓ Ошибка: "Port 8000 is already in use"

**Ответ**: Запустите на другом порту:
```bash
python manage.py runserver 8080
```

Или завершите процесс на порту 8000:
```bash
# Mac/Linux
lsof -i :8000
kill -9 <PID>

# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### ❓ Ошибка: "ModuleNotFoundError: No module named 'django'"

**Ответ**: Установите Django:
```bash
pip install Django
```

### ❓ Проблема: Таймер не работает

**Ответ**: 
- Проверьте консоль браузера (F12 → Console)
- Убедитесь, что JavaScript включен в браузере
- Попробуйте очистить кэш: Ctrl+Shift+Delete

### ❓ Проблема: Не видно стилей (CSS)

**Ответ**: Очистите кэш браузера:
1. Нажмите Ctrl+Shift+Delete
2. Выберите "Файлы cookie и другие данные сайтов"
3. Нажмите "Удалить данные"

### ❓ Проблема: Картинки не загружаются

**Ответ**: 
1. Заполните папку `static/images/`
2. Обновите пути в CSS/HTML
3. Перезагрузитесь: Ctrl+F5

---

## Безопасность

### ❓ Кто может войти в админ-панель?

**Ответ**: Только пользователи с флагом "is_staff" и "is_superuser". Вы создали его при `createsuperuser`.

### ❓ Как добавить другого администратора?

**Ответ**: 
```bash
python manage.py createsuperuser
```

Или в админ-панели: левое меню → "Пользователи" → "Добавить пользователя"

### ❓ Что будет, если кто-то угадает мой пароль?

**Ответ**: 
1. Измените пароль в админ-панели (правый верхний угол)
2. Проверьте логи (если есть)
3. Переподключитесь с новым паролем

### ❓ Каковы требования к паролю?

**Ответ**: Django проверяет:
- Минимум 8 символов
- Не совпадает с логином/email
- Содержит буквы и цифры

---

## Расширение функциональности

### ❓ Как добавить новый тип вопроса (множественный выбор)?

**Ответ**: 
1. Отредактируйте `test_pr/models.py` - добавьте поле `question_type`
2. Создайте миграцию и примените её
3. Обновите `test_pr/views.py` и `take_test.html`

### ❓ Как сделать загрузку результатов в CSV?

**Ответ**: Используйте Django модуль `csv` и создайте новый view

### ❓ Как добавить email уведомления?

**Ответ**: 
1. Установите `django-environ`
2. Настройте SMTP в `settings.py`
3. Добавьте код в `views.py`:

```python
from django.core.mail import send_mail

send_mail(
    subject='Результаты теста',
    message=f'Вы набрали {result.percentage}%',
    from_email='admin@example.com',
    recipient_list=[email],
)
```

---

## Производительность

### ❓ Приложение работает медленно с большим количеством тестов

**Ответ**: 
1. Добавьте индексы в модели:
```python
class Test(models.Model):
    title = models.CharField(max_length=255, db_index=True)
```
2. Используйте `select_related()` и `prefetch_related()`
3. Кэшируйте часто используемые данные

### ❓ Как оптимизировать запросы к БД?

**Ответ**: В `views.py` используйте:
```python
tests = Test.objects.select_related(
    'questions'
).prefetch_related(
    'questions__answers'
)
```

---

## Резервное копирование

### ❓ Как сделать резервную копию БД?

**Ответ**: 
```bash
# Mac/Linux - скопировать файл
cp db.sqlite3 db.sqlite3.backup

# Или используйте dump
python manage.py dumpdata > backup.json
```

### ❓ Как восстановить БД из резервной копии?

**Ответ**: 
```bash
# Перезагружаем данные
python manage.py loaddata backup.json
```

---

## Развёртывание

### ❓ Как развернуть приложение на сервер?

**Ответ**: Используйте:
- Gunicorn (WSGI server)
- Nginx (веб-сервер)
- PostgreSQL (БД в production)
- Systemd (автозапуск)

Документация: https://docs.djangoproject.com/en/4.2/howto/deployment/

---

## Поддержка

### ❓ Что делать, если я не нашел ответ?

**Ответ**:
1. Проверьте документацию Django: https://docs.djangoproject.com/
2. Посмотрите консоль ошибок (F12 в браузере)
3. Проверьте логи сервера в терминале
4. Поищите ошибку в интернете

---

**Если у вас есть другие вопросы - изучите файлы документации проекта! 📚**
