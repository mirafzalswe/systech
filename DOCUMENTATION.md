# 📚 Техническая документация

## Архитектура приложения

### Слои приложения:

```
┌─────────────────────────────────────────┐
│         Пользовательский слой            │  HTML/CSS/JavaScript
│     (Шаблоны Django)                    │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│       Слой представления (Views)         │  Django Views
│   (Логика обработки запросов)           │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│       Слой моделей (Models)             │  ORM Django
│    (Работа с базой данных)              │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│        SQLite База данных                │
└─────────────────────────────────────────┘
```

## Views (Представления)

### Пользовательские Views:

#### 1. `register(request)`
- **Метод**: GET, POST
- **URL**: `/`
- **Описание**: Регистрация участника перед началом теста
- **Параметры**: 
  - `first_name` (POST) - Имя участника
  - `last_name` (POST) - Фамилия участника
- **Возвращает**: HTML форму или перенаправляет на список тестов

#### 2. `test_list(request)`
- **Метод**: GET
- **URL**: `/tests/`
- **Описание**: Отображение списка активных тестов
- **Требует**: Активная сессия с participant_id
- **Контекст**:
  - `participant` - Объект текущего участника
  - `tests` - Список активных тестов
  - `completed_tests` - ID пройденных тестов

#### 3. `take_test(request, test_id)`
- **Метод**: GET, POST
- **URL**: `/test/<int:test_id>/take/`
- **Описание**: Прохождение теста
- **GET**: Показывает форму с вопросами
- **POST**: Обрабатывает ответы и создает TestResult
- **Параметры**: 
  - `answer_<question_id>` (POST) - ID выбранного ответа
- **Логика**:
  1. Проверяет наличие сессии
  2. Проверяет, не прошел ли уже тест
  3. Создает TestResult при завершении
  4. Подсчитывает правильные ответы
  5. Перенаправляет на страницу результатов

#### 4. `test_result(request, result_id)`
- **Метод**: GET
- **URL**: `/result/<int:result_id>/`
- **Описание**: Отображение результатов теста
- **Контекст**:
  - `result` - Объект TestResult
  - `user_answers` - Ответы пользователя
  - `test` - Объект теста

#### 5. `logout_user(request)`
- **Метод**: GET
- **URL**: `/logout/`
- **Описание**: Выход пользователя (очистка сессии)
- **Действие**: Очищает session и перенаправляет на register

### API Endpoints:

#### 1. `save_answer(request, test_id)` ⚠️
- **Метод**: POST (AJAX)
- **URL**: `/test/<int:test_id>/save-answer/`
- **Описание**: Сохранение ответа (для будущего использования)
- **Данные**: JSON с question_id и answer_id
- **Возвращает**: JSON `{"success": true/false}`

#### 2. `get_test_timer(request, test_id)`
- **Метод**: GET (AJAX)
- **URL**: `/api/test/<int:test_id>/timer/`
- **Описание**: Получение информации о таймере теста
- **Возвращает**: JSON с timer_minutes и timer_seconds

## Модели данных

### Test Model
```python
class Test(models.Model):
    title                    # CharField(255) - Название теста
    description             # TextField - Описание
    status                  # CharField(choices=['active', 'inactive'])
    show_answers            # CharField(choices=['after_each', 'at_end', 'never'])
    show_result             # BooleanField - Показывать ли результат
    timer_minutes           # IntegerField(nullable) - Таймер в минутах
    created_at             # DateTimeField(auto_now_add=True)
    updated_at             # DateTimeField(auto_now=True)
```

### Question Model
```python
class Question(models.Model):
    test                   # ForeignKey(Test) - Принадлежит этому тесту
    text                   # TextField - Текст вопроса
    order                  # IntegerField - Порядок в тесте
    created_at            # DateTimeField(auto_now_add=True)
```

### Answer Model
```python
class Answer(models.Model):
    question              # ForeignKey(Question) - Ответ на этот вопрос
    text                  # CharField(500) - Текст ответа
    is_correct            # BooleanField - Флаг правильного ответа
    order                 # IntegerField - Порядок ответа
    created_at            # DateTimeField(auto_now_add=True)
```

### Participant Model
```python
class Participant(models.Model):
    first_name           # CharField(100) - Имя участника
    last_name            # CharField(100) - Фамилия участника
    created_at           # DateTimeField(auto_now_add=True)
```

### TestResult Model
```python
class TestResult(models.Model):
    test                 # ForeignKey(Test)
    participant          # ForeignKey(Participant)
    total_questions      # IntegerField - Всего вопросов в тесте
    correct_answers      # IntegerField - Правильных ответов
    percentage           # FloatField - Процент (вычисляется)
    started_at          # DateTimeField
    completed_at        # DateTimeField(auto_now_add=True)
    is_completed        # BooleanField
```

### UserAnswer Model
```python
class UserAnswer(models.Model):
    test_result         # ForeignKey(TestResult)
    question            # ForeignKey(Question)
    selected_answer     # ForeignKey(Answer, nullable)
    is_correct          # BooleanField
    created_at          # DateTimeField(auto_now_add=True)
    updated_at          # DateTimeField(auto_now=True)
```

## Таймер - Логика работы

### Клиентская сторона (JavaScript):
```javascript
// Инициализируется на странице take_test.html
let timeLeft = timer_seconds;

function updateTimer() {
    const minutes = Math.floor(timeLeft / 60);
    const seconds = timeLeft % 60;
    // Отображение: "15:30"
    
    if (timeLeft <= 60) {
        // Желтое предупреждение
    }
    if (timeLeft <= 0) {
        // Красное предупреждение + автоматическое завершение
        form.submit();
    }
    timeLeft--;
    setTimeout(updateTimer, 1000);
}
```

### Серверная сторона (Django):
- Таймер установлен в модели Test (`timer_minutes`)
- Проверка таймера выполняется на клиенте
- При истечении времени форма автоматически отправляется

## Процесс прохождения теста

### Шаг 1: Регистрация
```
GET / → Форма регистрации
POST / → Создание Participant, сохранение participant_id в session
Перенаправление на /tests/
```

### Шаг 2: Выбор теста
```
GET /tests/ → Список активных тестов с проверкой пройденных
Клик на "Начать" → GET /test/<id>/take/
```

### Шаг 3: Прохождение
```
GET /test/<id>/take/ → Отображение всех вопросов
Пользователь выбирает ответы и навигирует
POST /test/<id>/take/ → Обработка ответов
```

### Шаг 4: Обработка результатов
```
1. Создание TestResult
2. Создание UserAnswer для каждого вопроса
3. Проверка is_correct для каждого ответа
4. Подсчет correct_answers
5. Расчет percentage
6. Перенаправление на /result/<result_id>/
```

### Шаг 5: Просмотр результатов
```
GET /result/<id>/ → Отображение результатов и ответов
```

## Администраторские функции

### Django Admin (/admin/)

#### 1. Test Admin
- Inline editor для Questions
- Список фильтров (status, created_at, show_answers)
- Поиск по title, description
- Настройка отображения результатов

#### 2. Question Admin
- Inline editor для Answers
- Отображение связанного теста
- Поиск по тексту вопроса
- Сортировка по порядку

#### 3. Answer Admin
- Фильтрация по is_correct
- Фильтрация по тесту
- Быстрое редактирование текста

#### 4. Participant Admin
- Просмотр количества пройденных тестов
- Поиск по имени/фамилии
- Список дат создания

#### 5. TestResult Admin (READ-ONLY)
- Детальный список результатов
- HTML таблица с ответами (collapse)
- Фильтрация по тесту и статусу
- Ограничение на редактирование/удаление

## Безопасность

### Реализованные защиты:

1. **CSRF Protection**: Все формы используют `{% csrf_token %}`
2. **Session Management**: Проверка participant_id в session
3. **Data Validation**: Валидация на сервере всех входных данных
4. **Uniqueness**: Один участник может пройти тест только один раз
5. **Read-Only**: Результаты нельзя редактировать через форму

### Потенциальные улучшения:

- [ ] Добавить User Authentication для администраторов
- [ ] Логирование всех действий
- [ ] Двухфакторная аутентификация
- [ ] Ограничение по IP адресу
- [ ] Шифрование критичных данных

## Расширяемость

### Добавление новых полей к тесту:

1. Отредактируйте `test_pr/models.py` - добавьте поле в Test
2. Создайте миграцию: `python manage.py makemigrations`
3. Примените миграцию: `python manage.py migrate`
4. Обновите форму в `test_pr/admin.py` - добавьте field в fieldsets

### Добавление новой страницы:

1. Создайте view в `test_pr/views.py`
2. Добавьте URL в `test_pr/urls.py`
3. Создайте template в `test_pr/templates/test_pr/`
4. Наследуйте от `base.html`

## Производительность

### Оптимизации:

- `select_related()` для ForeignKey в QuerySet
- `prefetch_related()` для ManyToMany и Reverse FK
- Кэширование результатов в шаблонах (`.cache_key`)

### Пример оптимизированного Query:

```python
result = TestResult.objects.select_related(
    'test',
    'participant'
).prefetch_related(
    'user_answers__question',
    'user_answers__selected_answer'
).get(id=result_id)
```

---

**Документация актуальна для версии Django 4.2**
